# MEG (Stable Diffusion + ControlNet + diffusers) — install when using MEG/
# Usage: pip install -r requirements-meg.txt
# Tested around diffusers 0.35.x; pin versions in your paper supplement if needed.

diffusers>=0.27.0
transformers>=4.36.0
accelerate>=0.26.0
safetensors>=0.4.0
huggingface_hub>=0.20.0
matplotlib>=3.3.0  # MEG_show.py figures
